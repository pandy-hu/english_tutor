# K12 英语辅导小工具 · 产品需求文档（PRD）

| 字段 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 创建日期 | 2026-08-03 |
| 负责人 | pandy（一人公司 / 零资金副业项目） |
| 状态 | ✅ 需求已固化（v0.1 已开发完成，待验收） |
| 配套仓库 | `english_tutor/`（本地 Git，commit b3b2428） |

> 本文档作用：把「要做什么、做到什么程度算合格」写死，作为后续开发、验收、加功能的唯一依据。改需求先改本文档，再动代码——这就是「先写说明书、再写代码」。

---

## 1. 背景与问题（为什么做）

K12（小学到高中，6–18 岁）学英语普遍卡在「学了不会用、没人纠、家长辅导不了」。前期调研梳理出的核心痛点：

| # | 痛点 | 归属角色 |
|---|---|---|
| 1 | 单词记了就忘，死背字母表、无复习节奏 | 学生 |
| 2 | 发音不标准没人纠，班额大老师顾不上 | 学生 |
| 3 | 语法抽象听不懂，刷题为主不知「为什么」 | 学生 |
| 4 | 听力抓不住词，语速快、连读弱读听不出 | 学生 |
| 5 | 口语没环境不敢开口，怕错怕丢人 | 学生 |
| 6 | 阅读遇生词就卡死，整篇读不下去 | 学生 |
| 7 | 写作中式英语、写完没人改 | 学生 |
| 8 | 刷题多提升慢、过程枯燥没动力 | 学生 |
| 9 | 家长自己英语差，检查不了作业、辅导不了 | 家长 |
| 10 | 好资源贵（外教/大机构动辄上万） | 家长 |
| 11 | 新课标+中高考听说比重上升，听说成必考 | 政策/机会 |
| 12 | 信息差：大量免费优质资源家长根本不知道 | 市场/机会 |

**机会判断**：K12 家长付费意愿极强，且普遍存在「不知道免费好资源」的信息差。本工具定位为**免费引流钩子 + 后续变现入口**（错题打印、1v1 答疑、资料包），比通用小工具更值钱。

---

## 2. 产品目标

**一句话定位**：一个零成本、可自托管的 K12 英语专项辅导助手，覆盖「背词→跟读→听力→阅读→语法→写作→口语→错题沉淀」完整学习闭环。

**目标分层**
- **用户价值**：让学生在家也能「被纠正、有反馈、记得住」，让家长「看得见孩子学了啥」。
- **商业价值**：用免费高频功能吸流量，错题本/报告做分享裂变与私域引流，后续接付费答疑/资料。
- **约束价值**：零资金启动、可一人维护、部署极简（推 GitHub 即公网）。

---

## 3. 目标用户与画像

| 角色 | 典型画像 | 核心诉求 | 付费敏感度 |
|---|---|---|---|
| 学生（主用） | 小高/初中为主，英语中下水平 | 有人纠音、有反馈、别太枯燥 | 不直接付费 |
| 家长（决策/买单） | 工薪家庭，自身英语弱 | 孩子「在学」、能看到进度、便宜/免费 | 高（但只认「有用」） |
| 自学者（长尾） | 成人补基础 | 免费、随时用 | 低 |

---

## 4. 核心功能需求（按模块）

> 优先级：P0=必须有；P1=重要；P2=增强。状态：✅已实现 / 🔲待做。

### 4.1 📇 单词卡（间隔重复记忆）— P0 ✅
- **功能**：按记忆曲线复习；答对→下次间隔变长，答错→立刻重来；掌握度≥60 自动进「已知词」。
- **用户故事**：作为学生，我想用科学节奏背词，这样记了不忘。
- **输入/输出**：词库（内置 222 词）+ 用户手动加词 → 卡片界面（单词/音标/发音/释义/例句/记得否）。
- **验收标准**
  1. 新词首次出现，点「还不会」后，下次复习间隔 < 点「记住了」的间隔。
  2. 单词语 `review_count≥3` 且 `mastery≥60` 时，进入已知词集合，阅读高亮不再标记它。
  3. 手动加词（`word`+`meaning` 必填）成功落库，重复词不重复添加。
  4. 待复习清空时显示鼓励文案。

### 4.2 🎙️ 跟读训练 — P0 ✅
- **功能**：听标准音 → 录音跟读 → 本地识别打分 → 错词提示。
- **用户故事**：作为学生，我想知道自己的发音和标准音差在哪，没人纠也能练。
- **输入/输出**：目标句（内置金句/自输）→ 麦克风录音 → 识别文本 + 匹配度% + 错漏词。
- **验收标准**
  1. 点「听标准发音」能播放 edge-tts 真人音（无网络时降级提示，不崩）。
  2. 录音后能返回识别文本（faster-whisper 就绪）或明确提示「识别未就绪」。
  3. 匹配度 = 文本相似度，范围 0–100；匹配度<70% 自动入错题本。
  4. 错漏词列表准确（基于词集合比对）。

### 4.3 👂 听力精听（听写填空）— P0 ✅
- **功能**：听一句 → 写听到的 → 看匹配度，练「抓词」。
- **验收标准**
  1. 材料可重复播放。
  2. 提交后给出匹配度；≥90 提示优秀，<60 展示原文并记错题。
  3. 不依赖外部服务，纯本地比对。

### 4.4 📖 阅读生词高亮 — P0 ✅
- **功能**：粘贴英文 → 生词高亮 + 一键查词；功能词（the/is/of…）不过度标记。
- **验收标准**
  1. 生词用 `<mark>` 高亮，已知词/高频功能词不高亮。
  2. 选词「查词」能从内置词库返回音标/释义/例句/发音；词库没有时给明确引导（去单词卡加 / 接 API）。
  3. 高亮结果用 `unsafe_allow_html` 安全渲染，不执行任意脚本。

### 4.5 📐 语法高频易错点 — P0 ✅
- **功能**：10 个高频易错点讲解 + 自测题。
- **验收标准**
  1. 讲解覆盖主谓一致、时态、冠词、介词等高频坑。
  2. 自测答错时展示解析并记错题；可「下一题」连续练。

### 4.6 📋 错题本 & 周报 — P0 ✅
- **功能**：各模块自动收集错题 → 周报图表 + CSV 导出（可打印/发家长）。
- **验收标准**
  1. 跟读/听力/语法/单词卡答错自动入本（来源、题目、作答、正确答案齐全）。
  2. 周报统计「本周/累计/模块分布」三指标 + 柱状图。
  3. CSV 导出用 `utf-8-sig` 编码，Excel 中文不乱码。
  4. 无错题时显示空态文案。

### 4.7 ✍️ 写作 AI 批改 — P1 🔲（接口就绪，配 key 启用）
- **功能**：粘贴作文 → LLM 诊断中式英语、给修改建议。
- **依赖**：`.streamlit/secrets.toml` 配置 `api_key/base_url/model`（DeepSeek 等，很便宜）。
- **验收标准**
  1. 未配置时显示配置说明，不报错、不影响其它模块。
  2. 配置后返回结构化批改（错误点 + 修改 + 理由），非纯翻译。

### 4.8 💬 口语 AI 陪练 — P1 🔲（接口就绪，配 key 启用）
- **功能**：按学生水平（小/初/高）用英语对话陪练。
- **验收标准**
  1. 未配置时降级提示。
  2. 配置后可多轮对话，AI 用词难度匹配所选学段。

---

## 5. 非功能需求

| 类别 | 要求 |
|---|---|
| **零成本约束** | 所有 P0 功能不依赖任何付费外部账号；AI 模块为可选增强，明确标注「配 key 启用」。 |
| **性能** | 页面首屏 < 3s；TTS 生成 < 3s；本地语音识别（tiny 模型）< 10s/句。 |
| **兼容性** | 主流浏览器（Chrome/Edge/Safari）+ 手机端可访问；录音用浏览器原生 `audio_input`。 |
| **数据安全/隐私** | 学习数据仅存本地 `progress.json` / `mistakes.json`，**不上传任何服务器**；隐私声明在「关于」页明示。 |
| **可部署性** | `streamlit run app.py` 即起；推 GitHub 后 Streamlit Cloud 一键公网。 |
| **健壮性** | 所有外部依赖（TTS/whisper/LLM）防御式导入；缺依赖时降级提示，**绝不崩页**。 |
| **可维护** | 核心逻辑集中在 `core.py`，页面在 `app.py`；新增模块遵循现有 Tab 模式。 |

---

## 6. 范围（Scope）

**✅ In Scope（v1）**
- 上述 9 个模块（6 免费 + 2 AI 接口 + 关于）。
- 内置 222 词 K12 核心词库，支持手动加词。

**❌ Out of Scope（当前版本不做）**
- 真人在线答疑 / 付费外教（后续变现层）。
- 教材全量词库（需接免费词典 API 批量导入）。
- 云端数据持久化存储（Streamlit Cloud 免费版无挂载目录/持久卷，数据会随实例重启清空；需后续接 CloudBase / 外部数据库实现真正持久化）。
- 家长端微信/邮件自动推送（路线图 v0.3）。
- 多用户账号体系（一人公司阶段不需要）。

---

## 7. 验收总览表

| 模块 | 优先级 | 状态 | 关键验收点 |
|---|---|---|---|
| 单词卡 | P0 | ✅ | 间隔逻辑、掌握度进已知词、手动加词 |
| 跟读 | P0 | ✅ | 发音播放、识别打分、<70%记错 |
| 听力精听 | P0 | ✅ | 可重播、匹配度分级、记错 |
| 阅读高亮 | P0 | ✅ | 生词高亮、功能词不过标、查词 |
| 语法 | P0 | ✅ | 易错点讲解 + 自测记错 |
| 错题本 | P0 | ✅ | 自动收集、周报、CSV(utf-8-sig) |
| 写作批改 | P1 | 🔲 | 配 key 启用、结构化批改 |
| 口语陪练 | P1 | 🔲 | 配 key 启用、分学段对话 |
| 数据持久化 | P2 | ✅(本地) / 🔲(云端) | 用户数据存 user_data/，本地重启不丢；Streamlit Cloud 免费版无挂载目录，云端持久化需后续接 CloudBase/外部数据库 |
| 家长端推送 | P2 | 🔲 | 周报推微信/邮件 |

---

## 8. 版本规划 / 里程碑

| 版本 | 内容 | 状态 |
|---|---|---|
| v0.1 | 6 免费模块 + 2 AI 接口（未启用）+ 错题本 + 词库 222 | ✅ 已完成（b3b2428） |
| v0.2 | ① 用户数据独立目录（本地持久化，云端临时目录运行）② 词库扩到 652 词 + 学段标签 + 单词卡按学段选词 | ✅ 本地完成（2026-08-03） |
| v0.3 | 家长端：周报自动推微信/邮件（变现钩子） | 🔲 |
| v1.0 | 公网部署 + 引流（小红书/公众号/九宫格玩法）+ 收款/赞助位 | 🔲 |

---

## 9. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 免费 TTS（edge-tts）限流/断网 | 发音失效 | 降级提示；可换本地 TTS 或缓存音频 |
| faster-whisper 本地识别慢/占内存 | 跟读卡顿 | tiny 模型；云端用 CPU 优化；无 GPU 仍可用 |
| Streamlit Cloud 重启清空本地数据 | 错题/进度丢失 | 短期接受临时存储；后续接 CloudBase / 外部数据库实现真正云端持久化 |
| AI 模块 key 成本 | 边际成本 | 明确标注可选；默认关闭；选最便宜模型 |
| 同质化竞品多 | 引流难 | 靠「免费+错题可分享给家长」差异化 + 信息差内容运营 |
| 一人维护精力有限 | 迭代慢 | 模块解耦，小步快跑，PRD 驱动优先级 |

---

## 10. 成功指标（KPI）

- **使用**：周活跃会话数、各模块使用占比（跟读/阅读应最高频）。
- **沉淀**：周均错题收集数、CSV 导出次数（衡量「家长可见」价值）。
- **增长**：分享/引流转化数、公众号/知识星球新增。
- **商业**：赞助/资料包转化（v1.0 后）。

---

## 11. 需求变更流程

1. 任何功能新增/修改，先在此 PRD 的对应章节更新（含验收标准）。
2. 提交 PRD 改动到 Git（commit message 含 `prd:` 前缀）。
3. 再按更新后的验收标准改代码、自测、提交。
4. 每次发版在「版本规划」表更新状态。

> 一句话纪律：**PRD 是合同，代码是履行。先改合同，再履约。**
